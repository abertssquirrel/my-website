{
  "name": "astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently --kill-others \"astro dev\" \"npm run server\"",
    "server": "bun run --watch server/index.ts",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.7.10"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
